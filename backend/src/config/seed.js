/**
 * JOYN Platform — REAL DATA ONLY POLICY
 * Automatic database seeding with mock users/activities is permanently disabled.
 */
export async function seedDatabase() {
  console.log('[db] automatic database seeding is disabled by JOYN REAL DATA ONLY policy.');
  return;
}
